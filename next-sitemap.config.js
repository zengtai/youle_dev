/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.SITE_URL || "https://www.uptapgames.com",
  generateRobotsTxt: true, // (optional)
  // ...other options
};

// export default config;
